import SingleElement from './SingleElement';

export class ZarandeoElement extends SingleElement {
	constructor(animation, pathStrings, figure) {
		super(animation, pathStrings, 'woman', figure);
	}
}
